# Sentinel Workbench POC
# Deterministic Phase 1 validation requires no external services and no secrets.
SENTINEL_WORKBENCH_MODE=deterministic

# Optional OpenRouter model comparison mode.
# Keep real values only in local .env; do not commit API keys.
OPENROUTER_API_KEY=
MODEL_1=
MODEL_2=
MODEL_3=
