# GOAL.md

## OUTCOME

Complete the completeness-scan remediation pass so Sentinel's checked documentation, verification tooling, and demo evidence are internally consistent with the current deterministic local workbench state.

The previous `GOAL.md` clinician-facing staged demo milestone is complete and is evidenced by:

- `docs/21_goal_completion_audit.md`
- `validation/reports/goal_completion_audit.json`
- `validation/reports/final_verification.json`
- commit `9be150a Add durable final verification report`

This new goal starts from the completeness scan performed after that milestone. It is a polish, hardening, and evidence-alignment goal, not a request to expand clinical functionality.

Goal shape: `completeness_scan_remediation`.

## COMPLETENESS SCAN FINDINGS TO RESOLVE

The scan identified no high-severity deterministic-demo blockers. The required work is to close the remaining medium and low gaps:

1. `README.md` still states the older 24/25 audit status even though the current audit is 25/25.
2. `docs/18_deterministic_poc_status.md` omits the newer final-verification and goal-audit commands from its verification command list.
3. `src/sentinel_workbench/final_verification.py` writes a transient all-pass bootstrap report to `validation/reports/final_verification.json`; an interrupted run could leave misleading local evidence.
4. The clinician-facing local UX has HTTP and HTML tests but lacks a current visual/browser verification artifact.
5. Older chronological `PROGRESS.md` entries say some now-completed items remain future work, which can confuse readers.
6. OpenRouter/model-swap wording is ambiguous: the comparison harness exists, while app-integrated LLM mode and formal model-swap evaluation remain deferred.

## PROOF OF DONE

This goal is complete only when all of the following are true and verified from the current worktree:

1. `README.md` accurately states that the current goal-completion audit reports 25/25 and points to `validation/reports/final_verification.json`.
2. `docs/18_deterministic_poc_status.md` includes the current final-verification and goal-audit commands, while preserving the not-production-ready and no-clinical-safety-claim boundaries.
3. `sentinel_workbench.final_verification` no longer leaves a plausible all-pass durable report if interrupted during bootstrap/self-verification.
4. Tests cover the hardened final-verification behavior, including the non-bootstrap committed report requirement.
5. A browser or equivalent rendered-UX verification artifact exists for the local staged app, with evidence that the first screen, review-question selection, pre-process page, node audit, ensemble section, process result, clinician summary, and deeper-dive links render without obvious layout breakage.
6. `PROGRESS.md` has a latest-status or supersession note making clear that earlier chronological pending/future-work entries may be superseded by later sections.
7. OpenRouter documentation distinguishes:
   - implemented comparison harness,
   - comparison-only model artifacts,
   - deferred app-integrated LLM mode,
   - deferred formal model-swap evaluation.
8. The final verification artifacts are regenerated after the remediation, and `validation/reports/goal_completion_audit.json` is either updated to the new goal shape or explicitly archived/superseded so it cannot be mistaken for proof of the new goal.
9. Run and confirm all relevant commands exit 0:

```bash
python3 -m pytest -q
PYTHONPATH=src python3 -m sentinel_workbench.validate data/cases
PYTHONPATH=src python3 -m sentinel_workbench.static_inputs --static-inputs data/static_inputs/static_inputs.json --case-dir data/cases
PYTHONPATH=src python3 -m sentinel_workbench.evaluate --case-dir data/cases --out validation/reports/latest.json --receipt-dir data/receipts
PYTHONPATH=src python3 -m sentinel_workbench.final_verification
git diff --check
```

10. Inspect current state and confirm:

```bash
git status --short --branch
rg -n "24 static-artifact passes|item 25 pending|leaves item 25 pending" README.md docs PROGRESS.md
rg -n "bootstrap" validation/reports/final_verification.json
```

The first command may show only intentionally untracked local files such as `.env` or review PDFs. The second command must return no stale current-state claims. The third command must return no committed bootstrap marker in the durable final-verification report.

## SCOPE

Modify only files needed to resolve the completeness scan findings, expected areas:

- `README.md`
- `docs/18_deterministic_poc_status.md`
- `docs/21_goal_completion_audit.md` or a clearly named successor/supersession note
- `PROGRESS.md`
- `DECISIONS.md` if a durable verification or UX-proof decision is made
- `src/sentinel_workbench/final_verification.py`
- local app/browser verification tests or helper scripts under `tests/` or `src/sentinel_workbench/`
- `validation/reports/*.json` only when regenerated by documented commands
- optional rendered UX evidence under a clearly named, non-secret artifact path

Read/reference:

- `GOAL.md`
- `DECISIONS.md`
- `PROGRESS.md`
- `docs/18_deterministic_poc_status.md`
- `docs/21_goal_completion_audit.md`
- `validation/reports/final_verification.json`
- `validation/reports/goal_completion_audit.json`
- `data/prepared_inputs/constructed_demo_case/analysis/review.html`
- local app tests in `tests/test_phase_g_local_demo_app.py`

Do not modify:

- `.env`
- raw PHI or real clinical data
- ignored OpenRouter raw response artifacts
- unrelated generated caches such as `__pycache__`, `.DS_Store`, or `.pytest_cache`
- sibling repositories unless a separate goal explicitly authorizes cross-repo work

## SAFETY / PROVENANCE

- Sentinel remains a local deterministic governance-review POC using synthetic or constructed/deidentified-style inputs.
- Do not add direct clinical recommendations, diagnosis logic, prescribing logic, ordering logic, clearance language, or patient-specific medical advice.
- Do not weaken forbidden-phrase checks, residual-risk gates, reviewer approval requirements, future-leakage prevention, node audit evidence, ensemble transparency, or receipt traceability.
- Do not turn OpenRouter output into graph authority or app authority in this goal.
- Do not claim production readiness, real clinical safety validation, regulatory compliance, real PHI readiness, clinical outcome benefit, or live evidence reliability.
- If a useful idea is larger than this remediation pass, document it as deferred instead of partially implementing it.

## CONSTRAINTS

- Preserve existing public CLI names unless a rename is required for safety and is documented.
- Preserve existing schema semantics and generated receipt structure unless a test-backed update is required.
- Do not remove or weaken tests to make the goal pass.
- Do not add large dependencies unless needed for browser verification and justified in `DECISIONS.md`.
- Prefer deterministic, local, reproducible verification over manual assertions.
- Keep documentation current-state statements aligned with machine-readable reports.
- Keep raw model responses and secrets out of committed artifacts.

## ITERATION

1. Start by confirming `git status --short --branch` and reading the source-of-truth files listed in `AGENTS.md`.
2. Work in small batches:
   - docs alignment,
   - final-verification hardening,
   - visual/browser verification evidence,
   - report regeneration and final audit.
3. After each batch, run the nearest relevant tests before expanding scope.
4. If generated reports change only in timing strings, either regenerate consistently as part of the final proof or avoid unnecessary churn.
5. Record meaningful decisions in `DECISIONS.md` and current evidence in `PROGRESS.md`.
6. Before completion, rerun the full proof command set and inspect the stale-claim grep results.

## STOP

Pause and report the current evidence if:

- browser verification requires a tool or dependency that is unavailable after one documented fallback attempt,
- final-verification hardening creates a self-verification cycle that cannot be resolved without weakening tests,
- a change would require handling real PHI, credentials, production deployment, or external clinical systems,
- the same verification failure persists after three distinct repair attempts,
- the required change would broaden the project into app-integrated LLM authority, live evidence retrieval, production hardening, or real clinical use.

## COMPLETE

Mark this goal complete only when every `PROOF OF DONE` item is satisfied by current command output or inspected artifacts.

Completion must include a final summary with:

- changed files,
- verification commands and results,
- remaining intentionally deferred items,
- current git status,
- whether any untracked local files remain and why they were not touched.
